import { SmartSlicePipe } from './smart-slice.pipe';

describe('SmartSlicePipe', () => {
  it('create an instance', () => {
    const pipe = new SmartSlicePipe();
    expect(pipe).toBeTruthy();
  });
});
